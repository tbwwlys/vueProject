import { baseURL } from '../../constants' ;
import { Item } from '../../../types/item'

export async function fetchFeed(
    id: string,
    widthComments = false
) :Promise<Item>{
    return new Promise(() => {
        
    })
}